// w a p that show all special operators in one print command.

#include<stdio.h>
void main(){
    int num;
    printf("Enter a num: ");
    scanf("%d",&num);
    
    printf("Num:\nsizeof(num) = %d\n* num = \n&num = %d",sizeof(num),&num);

        
}