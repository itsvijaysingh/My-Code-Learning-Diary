//Write a program that read some meters and convert them into kilometers (1000 meters = 1 kilometer).

void main(){
    int m;
    printf("Enter meters that would you like to convert into KILOMeters: ");
    scanf("%d",&m);

    printf("%d M in kILOMeters IS: %dKm",m,m/1000);
}